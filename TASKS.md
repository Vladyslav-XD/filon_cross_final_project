# TASKS — queue for Claude Code (Mocktail Finder 1.1)

Rules (see CLAUDE.md): work top-down, one task = one commit with a clear message, tick the box here and append an entry to `STATUS.md` when a task is done. `npx tsc --noEmit` clean before every commit. No `eas build`, `eas submit` or `git push` without Vlad's explicit ok. Keep UK spelling in the UI ("Favourites"). Do not change `.env.local`. Version stays `1.0.0` until 1.0 is approved by Apple — do not bump it yet.

- [x] 0. **Tag the reviewed state.** `git tag v1.0.0-build4` on the current commit (this is the build Apple is reviewing), then show `git log --oneline -5`. Create a branch `release/1.1` for the work below.

- [x] 1. **Delete own recipe.** On `RecipeDetailsScreen` for a user-created recipe (id from `myRecipes` store — not for TheCocktailDB drinks) add a "Delete recipe" action below "Share Recipe" (destructive style, red text). Confirm with `Alert.alert` ("Delete this recipe?" / Cancel / Delete). On confirm: `removeRecipe(id)` from `src/store/myRecipesSlice.ts`, `deleteRecipePhoto(recipe.imageUrl)` from `src/utils/recipePhotos.ts`, remove it from favourites via `FavoritesContext` if saved, then `navigation.goBack()`. Verify in the simulator: create a recipe with a photo, delete it, confirm the photo file is gone (`FileSystem.getInfoAsync`) and the list updates.

- [x] 2. **Edit own recipe.** Add "Edit recipe" next to Delete. Reuse `AddRecipeScreen`: accept an optional `recipe` route param and pre-fill photo, name, description, character tags, ingredients and steps; on save dispatch an `updateRecipe` action (add it to the slice, keep the same id) instead of `addRecipe`; keep the existing photo unless the user changes or removes it (delete the old file when replaced). Screen title "Edit Recipe", button "Save Changes". Verify: edit name and one ingredient, reopen, values persist after app restart.

- [x] 3. **Camera for recipe photos.** In `app.json` `expo-image-picker` plugin config set `cameraPermission` to "Mocktail Finder uses the camera only to take a picture for a recipe you create. Photos stay on your device." (keep `photosPermission`, keep `microphonePermission: false`). In `src/utils/recipePhotos.ts` add `takeRecipePhoto()` using `ImagePicker.requestCameraPermissionsAsync()` + `launchCameraAsync({ quality: 0.7, exif: false })`; if permission is denied, show an Alert explaining how to enable it in Settings. On the Add Photo button show an action sheet ("Take Photo" / "Choose from Library" / Cancel) — `ActionSheetIOS` on iOS. The simulator has no camera: verify the library path in the simulator and leave a note in STATUS.md that the camera path needs a TestFlight check on a real iPhone.

- [x] 4. **Persist the theme.** `src/context/ThemeContext.tsx`: keep three modes `system | light | dark`, store the choice in AsyncStorage (key `@mocktail-finder/theme`, add it to `src/storage/storage.ts` STORAGE_KEYS), load it in `App.tsx` together with the other hydration calls so there is no flash of the wrong theme. The header toggle switches light ↔ dark explicitly; add a way back to "system" (e.g. long-press on the toggle or a third state) — keep it simple and explain the choice in STATUS.md.

- [ ] 5. **Small fixes.** (a) `RecipeDetailsScreen`: "Add to Favorites" / "Remove from Favorites" → "Add to Favourites" / "Remove from Favourites". (b) Remove `ios.buildNumber` from `app.json` (EAS manages the build number remotely, `appVersionSource: remote`). (c) `RecipeDetailsScreen`: the title card is sticky and content scrolls underneath it without any separation — either make it non-sticky or add a background/shadow so the overlap reads intentionally; smallest clean fix wins. (d) Light photos hide the light status bar on details/random screens — add a subtle top gradient over the image or switch the status-bar style based on image brightness is overkill; the gradient is enough.

- [ ] 6. **Bundled drink photos — assets are in place (17 Sept).** `assets/drinks/<id>.jpg` (58 files, 900×900 JPEG, 2.9 MB total) and `src/data/drinkPhotos.ts` (`drinkPhoto(id)` → `ImageSourcePropType | undefined`) are committed by Cowork; `tsc` is clean. Your part: wherever a TheCocktailDB drink image is rendered — `RecipeCard`, `RecipeDetailsScreen`, `RandomScreen` (and the share text if it references the image URL) — use `drinkPhoto(recipe.id)` first and fall back to the existing `{ uri: resolveImageUri(recipe.imageUrl) }` when it returns undefined (user recipes and any id without a local file). Keep `resizeMode="cover"`. Do not touch user-recipe photos (`recipe-photo:` scheme). Verify in the simulator that cards, details and Surprise show the new photos, that a user-created recipe still shows its own photo, and that the list scroll stays smooth. Commit the assets and the map together with your change.

- [ ] 7. **Release prep — only after Apple approves 1.0.** Bump `expo.version` to `1.1.0`, write What's New (Cowork provides the text), `eas build --platform ios --profile production` after Vlad's ok, TestFlight on a real iPhone (camera!), then Cowork updates screenshots/ASC and Vlad gives the ok for `eas submit`.
